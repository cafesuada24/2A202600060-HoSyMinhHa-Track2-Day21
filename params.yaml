# Loai mo hinh: random_forest, gradient_boosting, logistic_regression
model_type: "gradient_boosting"

# Sieu tham so cho mo hinh (su dung phu hop voi tung loai)
n_estimators: 500
max_depth: 15
min_samples_split: 5
# C: 1.0 # Cho logistic_regression
